name: Deploy Fortress Platform API to test

on:
  push:
    branches:
      - dev
    paths:
      - "apps/auth/**"
      - "apps/api/**"
      - "apps/mcp/**"
      - "packages/contracts/**"
      - "tools/deployment-smoke.mjs"
      - "pwa-website/data/duas.json"
      - "package.json"
      - "package-lock.json"
      - "README.md"
      - "tsconfig.base.json"
      - ".github/workflows/platform-deploy-test.yml"
  workflow_dispatch:

concurrency:
  group: platform-api-test
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy test API Worker
    if: ${{ vars.CLOUDFLARE_DEPLOY_ENABLED == 'true' }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify platform
        run: npm run check

      - name: Apply D1 identity migrations
        run: npm run db:migrate:test --workspace @fortress/auth
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Apply additive D1 content migrations
        run: npm run db:migrate:test --workspace @fortress/api
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy Auth to Cloudflare test
        run: npm run deploy:test:ci --workspace @fortress/auth
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy API to Cloudflare test
        run: npm run deploy:test --workspace @fortress/api
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy MCP to Cloudflare test
        run: npm run deploy:test:ci --workspace @fortress/mcp
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Smoke test deployed services
        run: npm run smoke:test:services
