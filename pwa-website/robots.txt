# Fortress of Muslim -- https://fortressofmuslim.org
#
# Everything is open to crawlers, including the app's scripts, styles and data files. Search
# engines render pages the way a browser does, and a page whose JavaScript and CSS they are not
# allowed to fetch cannot be rendered: until 0.49.1 this file disallowed /js/, /css/, /data/ and
# /assets/, which is the opposite of Google's guidance. The files themselves do not end up in
# results -- they are not linked as pages and are not in the sitemap.
#
# Only production is indexed. The test site and the Cloudflare preview addresses answer with
# X-Robots-Tag: noindex (pwa-website/edge/worker.js), so this file does not need to say so.

User-agent: *
Allow: /

Sitemap: https://fortressofmuslim.org/sitemap.xml
